import type {CalendarAdapter} from './calendar-adapter';
import {fullCalendarAdapter} from './fullcalendar-adapter';
import {bigCalendarAdapter} from './bigcalendar-adapter';

/*
Switch calendar library here (ONE line).

Example:
export const calendarAdapter: CalendarAdapter = bigCalendarAdapter;
*/

// export const calendarAdapter: CalendarAdapter = fullCalendarAdapter;
 export const calendarAdapter: CalendarAdapter = bigCalendarAdapter;
